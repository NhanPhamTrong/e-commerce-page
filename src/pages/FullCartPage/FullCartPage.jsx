export const FullCartPage = ({
    isMobileSize
}) => {
    return (
        <div id="full-cart-page">
            <main style={{ paddingTop: isMobileSize ? 128 : 80 }}>

            </main>
        </div>
    )
}